# Copy to .env and fill in. Load it with: export $(cat .env | xargs)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the vault location (default ./my-vault)
# SYNAPSE_VAULT=/Users/you/synapse-vault

# Google Calendar (see docs/gcal-setup.md)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
